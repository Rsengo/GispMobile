import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    fab: {
        backgroundColor: '#fff',
        position: 'absolute',
        right: 5,
        bottom: 5
    }
});

export default styles;