import { MAP_TYPES } from 'react-native-maps';

const initialState = {
    mapType: MAP_TYPES.STANDARD,
    searchData: [],
    geoJson: null,
    region: null
};

export default initialState;